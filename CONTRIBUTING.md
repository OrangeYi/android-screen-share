# 参与贡献

感谢你愿意改进 Android Screen Share。

提交问题时，请提供 Windows 版本、Android 版本、手机品牌、连接方式、所选模式，以及界面“运行状态”中的错误文字。公开日志前请自行遮挡设备序列号、IP 地址等个人信息。

提交代码前请确认：

1. 不要写死个人设备序列号、型号、IP 或本机路径。
2. 新增 ADB 命令必须显式使用用户选择的设备序列号。
3. 普通镜像和桌面模式必须保持独立停止，避免误关另一会话。
4. 新增界面文字优先放入 `locales/zh-CN.json`。
5. 修改通知助手后，用 JDK 17、Android SDK 和 Gradle 8.9 执行 `gradle :app:assembleDebug`。
6. 不要提交 `tools/scrcpy`、Gradle 缓存、签名密钥或包含个人信息的日志。

Pull Request 请简要说明改动目的、测试过的设备/系统，以及是否影响普通镜像、桌面模式、无线连接或通知停止功能。

