# 更新日志

本项目遵循语义化版本号。最新版本号也可在根目录的 `VERSION` 文件中查看。

## 1.3.1 - 2026-09-22

### 改进

- 将手机通知助手改为完全可选组件。
- 用户拒绝安装、APK 缺失、系统版本不兼容或安装失败时，仍会继续启动 scrcpy 投屏。
- 通知助手不可用时仅关闭手机通知、状态页和手机端停止按钮，不影响电脑端画面与控制。
- 通知助手运行通道初始化失败时自动回退到无通知投屏，不再中断整个投屏流程。
- 为 Android 12 等不支持 `cmd display get-displays` 的系统增加虚拟显示器编号识别回退，并正确发送 Home 启动指令。

## 1.3.0 - 2026-09-22

### 新增

- 新增手机端“屏幕分享状态”页面，可从应用列表或分享通知进入。
- 状态页直接检测普通手机镜像与桌面/虚拟显示器的电脑控制通道。
- 状态页支持分别停止两种投屏，并提供通知设置入口。

### 改进

- 分享仍在运行时，每 5 秒恢复一次被系统或用户划掉的前台通知。
- 通知服务被系统回收后会请求恢复，并重新投递启动参数。
- 自动记录每次会话的实际控制端口，兼容自定义端口和多个投屏模式。
- 通知助手升级为 1.1（versionCode 2）；电脑端会自动检测并更新旧版助手。
- 首次升级可自动迁移旧版一次性签名的通知助手，无需手动卸载。
- README 增加通知消失、状态确认和 Android“活动中的应用”的说明。

### 验证

- 已通过 Android 助手 Gradle 构建、APK 签名与清单检查。
- 已通过 PowerShell 语法检查和仓库个人信息扫描。
- 已在 Android 设备上验证状态识别、通知显示及手机端停止分享流程。

## 1.2.2 - 2026-09-21

- 移除自动全屏逻辑，恢复由 scrcpy 原生 `F11` 控制全屏，避免桌面模式任务栏消失。

## 1.2.1 - 2026-09-21

- 调整桌面模式全屏处理，尝试保留 DeX/桌面底部任务栏。

## 1.2.0 - 2026-09-21

- 新增全屏选项及设置记忆（该自动全屏功能随后在 1.2.2 中移除）。

## 1.1.1 - 2026-09-21

- 使用无控制台启动方式，避免运行时出现空白 CMD 窗口。

## 1.1.0 - 2026-09-21

- 新增程序内一键更新。
- 自动保存上次选择的设备、连接地址和投屏参数。

## 1.0.0 - 2026-09-21

- 首个公开版本。
- 提供 USB、无线 ADB、普通镜像和桌面/虚拟显示器模式。
- 提供中文图形界面、安装脚本和手机端分享通知助手。
