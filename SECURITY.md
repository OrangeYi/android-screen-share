# 安全与隐私

- 启动器没有遥测，不会上传设备信息。
- ADB 投屏流量只在所选电脑和 Android 设备之间传输。
- 无线 ADB 权限很高，请只在可信的私人网络中启用。
- 如需撤销授权，可在手机的“开发者选项 → 撤销 USB 调试授权”中操作。
- 可选通知助手只创建前台通知，并通过 ADB 反向映射的回环端口发送本地 `STOP` 消息。
- 通知助手的网络权限只用于 `127.0.0.1` 本地套接字，不访问公网。
- 项目不要求三星账户、Google 账户、Root 权限或解锁引导加载程序。
- 请勿提交签名密钥、设备序列号、IP 地址、配对码或本地缓存文件。

如需报告安全问题，请使用 GitHub Security Advisory，不要在公开 Issue 中披露漏洞细节。
